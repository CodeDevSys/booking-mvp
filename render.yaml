services:
  - type: web
    name: booking-mvp
    env: node
    plan: free
    buildCommand: npm install --omit=optional
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: ADMIN_KEY
        value: "123456"
